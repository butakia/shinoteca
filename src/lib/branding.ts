// Single source of truth for the site logo. Change these paths (or override
// via BrandingContext from the admin panel) to swap the logo everywhere at
// once — every Logo usage reads from here, none hardcode a path.
export const DEFAULT_BRANDING = {
  siteName: "SHINOTECA",
  logoFullSrc: "/brand/logo-full.png",
  logoIconSrc: "/brand/logo-icon.png",
  logoFullAspect: 2172 / 724,
  logoIconAspect: 518 / 408,
};
